const prompt = require("prompt-sync")();

let a = 1; // global variable //eveyone can access and reused in local;
let b = 2;

function sub() {
 let a = 5; 
 let c = 2;  
 let d = 2;  
           //local variable // its only within;
}
