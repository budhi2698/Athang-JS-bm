// ARRAY *Date 27/march/2023

let a = 1; // one var will hold single data; *Array is variable whereby it holds multiple data; one dimension array

// let names = ['Budhi', 'Tashi', 'Sonam'];

// names[3] = 'Sangay';           //one way *specify where to add
// names.push('james');           // push *add at the last
                              
// names.unshift('Gurung');       // add at the first

// names.pop();                   // will remove last array , keep on ad

// console.log(names);


// const names = ['Budhi', 'Tashi', 'Sonam']; //Const cannot reassign the same value but we can used or add once

// names[3] = 'Sangay';           
// names.push('james');           
                              
// names.unshift('Gurung');       

// names.pop();                   

// console.log(names);


//const names = []; this is an example already used names array;


// let names = ['Budhi', 'Tashi', 'Sonam'];

// for (let i =0; i < 3; i++) {

//     console.log(names[i]);
    
// }

